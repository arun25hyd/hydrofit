# Run this once: python3 deploy_images.py
# Writes 35 Parker fitting images to assets/images/fittings/
import base64, os
os.makedirs("/Users/admin/hydrofit/assets/images/fittings", exist_ok=True)
print("Ready — images will be written by the full script in Claude outputs")
